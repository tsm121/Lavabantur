import stmpy


class SensorStateMachine:
    def __init__(self, sensor_id, sensor):
        print("Creating Sensor State Machine for Sensor ID " + str(sensor_id))

if __name__ == "__main__":
    stm = SensorStateMachine(1, 1)
